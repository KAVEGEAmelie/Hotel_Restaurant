APP_NAME="Hotel Restaurant Le Printemps"
APP_ENV=production
APP_KEY=base64:Xmj2ol9FNXacWfCgPDNCNacKvWngXgbmrL2j6EUWc+0=
APP_DEBUG=false
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_LEVEL=info

# --- Base de données PostgreSQL Railway ---
DB_CONNECTION=pgsql
DB_HOST=${PGHOST}
DB_PORT=${PGPORT}
DB_DATABASE=${PGDATABASE}
DB_USERNAME=${PGUSER}
DB_PASSWORD=${PGPASSWORD}

# --- Alternative si tu veux MySQL ---
# DB_CONNECTION=mysql
# DB_HOST=${MYSQLHOST}
# DB_PORT=${MYSQLPORT}
# DB_DATABASE=${MYSQLDATABASE}
# DB_USERNAME=${MYSQLUSER}
# DB_PASSWORD=${MYSQLPASSWORD}

# --- Sessions, Cache ---
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

# --- Redis (si tu veux l’utiliser un jour) ---
REDIS_HOST=${REDISHOST}
REDIS_PASSWORD=${REDISPASSWORD}
REDIS_PORT=${REDISPORT}

# --- Mail (SMTP, exemple Yahoo Mail) ---
MAIL_MAILER=smtp
MAIL_HOST=smtp.mail.yahoo.com
MAIL_PORT=587
MAIL_USERNAME=hotelrestaurantleprintemps@yahoo.com
MAIL_PASSWORD=ton_mot_de_passe_application   # <- pas ton vrai mot de passe Yahoo, mais un mot de passe généré
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="hotelrestaurantleprintemps@yahoo.com"
MAIL_FROM_NAME="Hotel Restaurant Le Printemps"

# --- AWS (si besoin) ---
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# --- Pusher (temps réel si utilisé) ---
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# --- Frontend ---
VITE_APP_NAME="${APP_NAME}"
